export const HEADER_HEIGHT = 100;
export const HEIGHT_BANNER = 200;
export const MIN_HEIGHT_FOOTER = 300;

export const chairs = {
    a: Array.from({ length: 22 }, (_, i) => `a${i + 1}`),
    b: Array.from({ length: 22 }, (_, i) => `b${i + 1}`),
    c: Array.from({ length: 22 }, (_, i) => `c${i + 1}`),
    d: Array.from({ length: 22 }, (_, i) => `d${i + 1}`),
};
